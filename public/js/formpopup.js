$(document).ready(function () {    $('form').submit(function (event) {        var json;        event.preventDefault();        $.ajax({            type: $(this).attr('method'),            url: $(this).attr('action'),            data: new FormData(this),            contentType: false,            cache: false,            processData: false,            success: function (result) {                json = jQuery.parseJSON(result);                if (json.url) {                    Swal.fire({                        type: json.type,                        title: json.title,                        html: json.text,                        timer: json.timer * 1000,                        showConfirmButton: false,                        onClose: () => window.location.href = json.url                    })                } else if (json.submit_url) {                    Swal.fire({                        title: json.title,                        text: json.text,                        type: json.type,                        showCancelButton: true,                        confirmButtonColor: '#3085d6',                        cancelButtonColor: '#d33',                        confirmButtonText: json.submit_text,                        cancelButtonText: json.cancel_text                    }).then((result) => {                        if (result.value) {                            Swal.fire({                                title: 'Удалено!',                                text: 'Статья была удалена.',                                type: 'success',                                onClose: () => window.location.href = json.submit_url                            })                        }                    })                } else {                    Swal.fire({                        type: json.type,                        title: json.title,                        text: json.text                    })                }            }        })    })});function delete_commentary_confirm(url) {    Swal.fire({        title: 'Вы уверены?',        text: "You won't be able to revert this!",        type: 'warning',        showCancelButton: true,        confirmButtonColor: '#3085d6',        cancelButtonColor: '#d33',        confirmButtonText: 'Да, удалить!'    }).then((result) => {        if (result.value) {            window.location.href = url        }    })}